PROJECT: Big7Construction
GOAL:    Add `tests/test_lane_ctas.py` (+ `make test-lane-ctas`) that walks the three lane pages, extracts every `href` targeting `/?...#contact`, and asserts every `intent=service:*` slug is a live key in `index.html`'s `INTENT_TO_TYPE` map, every `type=<val>` is a live projectType radio value on `index.html`, and every `src=<slug>` is on the whitelisted lane-attribution set (home-repair-lane / commercial-industrial-lane / residential-construction-lane) that matches the page it sits on. Plus each lane page must expose ≥ MIN_LANE_CTAS quote-form CTAs so a slow gutting of the funnel can't ship silently. `--selftest` mutates 6+ ways (bad slug, wrong src for page, deleted mapping entry, radio-only path drift, dropped CTA below floor). `make test-lane-ctas` PASS + full `make test` PASS. Commit locally, no push. Log to Cockpit.
BUDGET:  25 min (auto-improve tick 20/9999)
STARTED: 2026-07-13
