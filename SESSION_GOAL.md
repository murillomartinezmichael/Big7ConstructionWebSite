PROJECT: Big7Construction
GOAL:    Conversion audit per CONVERSION_STANDARDS.md — top gap on the site is that the primary "Request a bid" CTAs (nav, hero, mobile menu, closer) plus adjacent conversion buttons (compliance packet, portfolio-request, scoping call, FAQ, testimonials CTA, process station) all hit `#contact` but carry no `data-intent`, so clicks fire no attribution and `position` is not tracked. Fix: add `data-intent="bid:<position>"` (or `book:` for booking actions) to all 10, extend the click IIFE to derive & emit `position` in `cta_click` payload, and lock the shape with `tests/test_primary_ctas.py` (stdlib) with `--selftest` covering 5 known-broken mutations. Full test suite (10 files) still PASS. Commit locally, no push. Log to Cockpit.
BUDGET:  25 min (auto-improve tick 16/9999)
STARTED: 2026-07-07 (dynamic)
