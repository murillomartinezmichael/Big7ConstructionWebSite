PROJECT: Big7Construction
GOAL:    Rung 2 PROVE fifth bite (tick 12) — brief options 1/2/3 all shipped (JSON-LD ticks 2/4/6/11, conversion loop tick 3, OG card tick 4). "Extra rep" per Book VIII: lock the tick-4 OG/Twitter tag fix against silent regression. Add `tests/test_og_twitter.py` — stdlib meta-tag parse of BOTH `index.html` and `404.html`, assert required OG + Twitter tag set (og:type/title/description/url/image + twitter:card/title/description/image), assert `og:image` references the branded `og-card.png` and NOT the 206px `jobsite-01.jpg` placeholder, assert `og:image:width == 1200` / `og:image:height == 630` / `og:image:type == image/png`, assert `twitter:card == summary_large_image`. `--selftest` mode with ≥10 broken-mutation cases; wire into `make test-og` + `make test` chain. All four suites still PASS end-to-end. Commit locally, no push.
BUDGET:  25 min (auto-improve tick 12/9999)
STARTED: 2026-07-07 (dynamic)
