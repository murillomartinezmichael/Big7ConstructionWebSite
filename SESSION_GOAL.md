PROJECT: Big7Construction
GOAL:    Rung 2 PROVE sixth bite (tick 13) — brief options 1/2/3 all shipped + locked (ticks 2/3/4/6/7/10/11/12). "Extra rep" per Book VIII: close the exact gap flagged in STATUS.md L15 — meta-tag/JSON-LD asserts today verify URL strings but not the byte at the URL, so `git rm images/og-card.png` would still pass every existing test. Add `tests/test_assets.py` — stdlib walk that collects every image URL referenced by JSON-LD `image`/`logo` on `index.html` PLUS `og:image`/`twitter:image` on `index.html` + `404.html`, resolves each URL back to a local path via the canonical origin, asserts file exists + non-empty + magic bytes match the extension (PNG `\x89PNG`, JPEG `\xff\xd8\xff`). `--selftest` with ≥8 broken-mutation cases. Wire into `make test-assets` + `make test` chain. All five suites still PASS. Commit locally, no push.
BUDGET:  25 min (auto-improve tick 13/9999)
STARTED: 2026-07-07 (dynamic)
