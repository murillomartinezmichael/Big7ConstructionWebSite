# Changelog

All notable changes to this project.

Format: [Keep a Changelog](https://keepachangelog.com/en/1.1.0/).
Adheres to [Semantic Versioning](https://semver.org/spec/v2.0.0.html).

## [Unreleased]

### Added (2026-07-20 — competitor-research follow-through)
- `docs/big7-capability-statement.pdf` — one-page capability statement compiled strictly from the already-published `#credentials` cred-table (license, $5M GL, bonds to $25M, warranty, safety, associations, track record). Repointed `commercial-industrial.html`'s "Request the compliance packet" CTA to the real file (was routing to the contact form with nothing to send); locked file-exists + href-points-at-it in `tests/test_conversion.py` (`check_compliance_packet_link`). Added to the Dockerfile fallback's COPY block (Cloudflare/wrangler serves the whole repo dir already, so no config change needed there).
- Commercial hero rewritten to lead with the flagship niche (warehouse/distribution + tenant fit-out) and promote the page's own pf-foot numbers (500+ projects, $180M+ built, zero OSHA recordables/3yr) into the hero lede — no new claims, just repositioned already-published copy.
- New residential FAQ item ("What does a project typically cost?") surfacing the existing budget-chip ranges + the two real portfolio numbers ($740K addition, $1.9M custom build) as a skimmable answer before a visitor reaches the form. Mirrored into the FAQPage JSON-LD (now 6 Q&As, locked by `tests/test_jsonld.py`).

### Changed
- **Two-path restructure (2026-07-17).** Homepage rebuilt as a lean chooser (hero + two path cards + trust strip + lean contact, no form; 3,244 → ~1,650 lines); `commercial-industrial.html` and `residential-construction.html` rebuilt as full destination pages on the index brand (Fraunces/Barlow Condensed/Inter, async fonts) each carrying services, portfolio, FAQ (+ FAQPage JSON-LD) and its **own tailored Formspree intake form** (lane-specific projectType radios, `_subject`, hidden `source` default). `home-repair.html` retired: 301 → `/residential-construction.html#home-repair` (nginx + `_redirects`), content and JSON-LD offers absorbed by the residential page; sitemap/Dockerfile/404/footers/OfferCatalog pruned to 2 lanes. Money-path JS extracted from index.html to shared **`/big7.js`** (`page` analytics key from pathname; hidden-source overwrite on `?src=`); inline head shim on index redirects legacy `/?intent=` money URLs to the owning lane form. Every submit now also mirrors fire-and-forget to the n8n `big7-lead` webhook (Formspree stays path-of-record); the n8n workflow + leads table were extended to the new field shape and verified live. All 21 test suites migrated to the 2-lane contracts (per-lane form specs, rebuilt-lane CTA floors, shim sync lock, per-page anchors/images, lane FAQPage agreement, 301 container assertion).

### Added
- **Rung II PROVE — intake form structural contract test (tick 16).** `tests/test_form.py` + `make test-form` (wired into `make test` alongside the seven prior stdlib suites). Closes the exact money-path gap `test_conversion.py` leaves open: the CTA→radio wiring is locked but nothing else asserts the form itself still works. New test slices the single `<form class="cform">` on `index.html`, parses every input's attributes (both quoted values AND bare boolean tokens like `required`), and asserts a 10-point contract: (1) exactly one `.cform` form on the page; (2) `action` matches `https://formspree.io/f/<id>` (endpoint drift silently drops intakes); (3) `method="POST"`; (4) `onsubmit="submitForm(event)"` handler wired (the fetch that keeps the visitor on-page); (5) required contact inputs — `name` (text/autocomplete=name), `email` (email/autocomplete=email), `phone` (tel/autocomplete=tel) — each carry `required` (browsers accept blank submissions without it, and password managers + mobile keyboards rely on the correct `autocomplete`); (6) projectType radios above floor (4) with `required` on at least one member (browsers only enforce required-of-group if a member declares it); (7) honeypot `name="website"` inside a wrapper hidden by `display:none` OR `aria-hidden="true"`, with `tabindex="-1"` + `autocomplete="off"` (removed honeypot → Formspree quota burns on spam within a week); (8) hidden Formspree control fields `_subject` (non-empty value) + `_replyto`; (9) alt-call `<a href="tel:...">` present inside the form (the "or call" fallback for visitors who won't fill the form); (10) tel: last-10-digits equals JSON-LD `telephone` last-10-digits (guards "Google shows one number, the page dials another" — robust to `+1-` prefix / punctuation differences via the last-10-digits normalization). `--selftest` mutates a synthetic-but-realistic baseline (matching the real form's shape: Formspree action, three required contact fields, `MIN_RADIOS` radios with `required` on the first, honeypot in a `display:none` wrapper, hidden Formspree controls, alt-call tel link, JSON-LD with matching telephone) 14 known-broken ways: Formspree action drifted to a dev URL, method downgraded to GET, `submitForm` handler stripped, `required` removed from phone, email type downgraded to text, name autocomplete stripped, radios collapsed below floor, radio `required` stripped from every member, honeypot removed entirely, honeypot wrapper unhidden, honeypot tabindex removed, hidden `_subject` control deleted, alt-call `tel:` link removed, phone drift between `tel:` and JSON-LD. All 14 caught with the *right* error substring — a mutation caught by the wrong assertion fails the selftest (rubber-stamp guard). Python 3.11+ stdlib only (`json`, `re`, `pathlib`, `sys`). `Makefile` gains `.PHONY test-form` + `test: test-jsonld test-seo-files test-conversion test-og test-assets test-anchors test-nginx test-form` — full eight-suite `make test` runs end-to-end PASS. First-draft bug caught in the writing: initial `_attrs()` regex matched only `name="value"` pairs and reported false failures on the live form's `required` boolean attributes; fixed by rewriting to handle both quoted-value AND bare-token attrs (with a leading `<tagname` strip so the tag name itself isn't parsed as a boolean attribute). Rationale: STANDARDS §3 requires tests on money paths — this is the single money-path surface where every CTA on the page ends, and the tick-16 brief explicitly named "every Get a Quote button must go to a working form that captures name/phone/project-type" as the audit target.
- **Rung II PROVE — on-disk asset existence + magic-byte contract test (tick 13).** `tests/test_assets.py` + `make test-assets` (wired into `make test` alongside the four other stdlib suites). Closes the exact gap STATUS.md L15 flagged after tick 12: every prior test asserts the URL string of the referenced asset, but none opens the byte at the URL, so a `git rm images/og-card.png` (or a rename typo in a future edit) would leave every URL-string test green while social share previews 404 and Google's rich-result crawler drops the logo. New test walks every image URL referenced in `index.html` — JSON-LD `image` (string or list), JSON-LD `logo.url` / `logo.contentUrl`, `og:image`, `twitter:image` — plus `og:image` + `twitter:image` on `404.html`. Strips the canonical origin (parsed from `<link rel="canonical">`) and resolves each URL to a local path under repo root, then asserts (a) file exists, (b) is non-empty, (c) magic-byte prefix matches the extension (PNG `\x89PNG\r\n\x1a\n`, JPEG `\xff\xd8\xff`). Catches "renamed .jpg to .png without re-encoding" and truncated-download regressions in addition to the primary `git rm` case. `--selftest` mutates 8 known-broken cases against a scratch temp-dir tree (missing file, wrong subdir, empty file, wrong-magic PNG, JPEG bytes renamed to `.png`, wrong-origin URL, http-not-https, relative URL) — all 8 caught, baseline PASS. Python 3.11+ stdlib only (`json`, `re`, `pathlib`, `urllib.parse`, `tempfile`). Full five-suite `make test` (JSON-LD + selftest → SEO files → conversion + selftest → OG/Twitter + selftest → assets + selftest) runs end-to-end PASS. `tests/README.md` table row 5 added with the `git rm` regression case explicitly named; footer bumped from four to five tests. Rationale: STANDARDS §3 requires tests on money paths — social share previews and JSON-LD logo/image are the money-signal surfaces, and a broken preview turns high-intent shares into blank-preview link rot. Ticks 6/7/10/11/12 all closed URL-string surfaces; this closes the underlying byte-level surface those tests assume without proving.
- **Rung II PROVE — OG + Twitter card contract test (tick 12).** `tests/test_og_twitter.py` + `make test-og` (wired into `make test` alongside the four other stdlib suites). Parses every `<meta>` tag on BOTH `index.html` and `404.html` with a matched-quote regex (backreference so an apostrophe inside a double-quoted `content` string — e.g. `"This isn't live."` on `404.html` — doesn't corrupt parsing). Asserts required OG set (`og:type`, `og:title`, `og:description`, `og:url`, `og:image`) + Twitter set (`twitter:card`, `twitter:title`, `twitter:description`, `twitter:image`) is present, `og:url` + `og:image` + `twitter:image` are absolute `https://` URLs, `og:image` + `twitter:image` reference the branded `og-card.png` and NOT the 206px `jobsite-01.jpg` placeholder (locks the tick-4 fix — reverting the placeholder is a silent-money-signal regression that would still pass a "meta tag exists" smoke), `og:image:width == 1200` / `og:image:height == 630` / `og:image:type == image/png`, and `twitter:card == summary_large_image` (any other value crops the 1200×630 card in every downstream client). `--selftest` mode mutates a minimal-but-valid baseline 12 known-broken ways (og:title/og:image/twitter:card/twitter:image missing, og:url downgraded to http, og:image reverted to the 206px placeholder, og:image is a random file that isn't the branded card, twitter:card downgraded to `summary`, og:image:width/height/type wrong, twitter:image reverted to placeholder) — 12/12 caught, baseline PASS. Python 3.11+ stdlib only (`re`, `pathlib`, `sys`). Full four-suite `make test` (JSON-LD + selftest → SEO files → conversion + selftest → OG/Twitter + selftest) runs end-to-end PASS. `tests/README.md` table row 4 added with the placeholder-regression lock explicitly named; footer bumped from three to four tests. Rationale: STANDARDS §6 lists "meta/OG tags real (paste URL into a chat app — does the preview look pro?)" as a deploy gate. Tick 4's branded card was the highest-effort SEO win in the log; without a guard, a future edit could revert `og:image` to the placeholder and no one would notice until someone shared a link on iMessage.
- **Rung II PROVE — JSON-LD `image` + `logo` locked in `test_jsonld.py` (tick 11).** Extended `REQUIRED_TOP` to include `image` and `logo` (previously unenforced by the test). New assertions: `image` must be a non-empty https URL string or list, every list entry must be `str` starting with `https://`; `logo` accepts either a raw https URL string or a `schema.org/ImageObject` dict with `@type == "ImageObject"`, `url` (or `contentUrl`) as absolute https, `width` int ≥ 112 (Google's floor for organization logo), and positive int `height`. Added `--selftest` mode: constructs a minimal-but-valid baseline block via `_valid_block()`, then applies 10 targeted mutations (`image` missing / empty / non-https / non-string; `logo` missing / string-not-https / dict-missing-@type / width-below-min / missing-height / wrong-type) and requires the assertion function to reject every one. Baseline passes, 10/10 mutations caught. `Makefile` `test-jsonld` target now runs both the golden check AND the selftest (matches `test-conversion` pattern from tick 10), so every `make test` invocation proves the assertions still bite. Purpose: lock the tick-11 JSON-LD image/logo upgrade against silent future regression, and close the "logo was a 206px placeholder" gap discovered while auditing the three tick-11 brief options.
- **Rung II PROVE — stdlib conversion IIFE contract test.** `tests/test_conversion.py` + `make test-conversion` (wired into `make test`). Locks the CTA-attribution money path against silent regression. `check()` parses the `const INTENT_TO_TYPE = { ... }` block in `index.html` (~line 2876), all `<input type="radio" name="projectType" value="...">` radios, and every `data-intent="service:*"` / `data-intent="portfolio:*"` on `<a>` elements. Asserts a four-way contract: (a) every mapping value is a real radio value; (b) every CTA intent has a mapping entry; (c) every mapping key is used by ≥1 CTA (dead mapping = removed CTA or drifted slug); (d) every radio value is a mapping target (orphan radio = dead code or missing CTA). Also asserts substring presence of `track('cta_click'`, `track('intake_submit'`, `dataLayer`, `PREFILL_MARK`, `window.gtag`, `window.plausible` — the attribution loop end-to-end. Passes today: 13 mapping entries, 13 CTA intents, 6 projectType radios. `--selftest` mode mutates the golden HTML 8 known-broken ways (renamed radio value, deleted mapping entry, stealth-removed CTA, orphan radio, `dataLayer`/`window.gtag` stripped, `track('cta_click'` call swapped, `INTENT_TO_TYPE` block deleted) and requires the RIGHT error substring to appear for each — not just a nonzero exit — so a mutation caught by the wrong assertion still fails the selftest. `make test-conversion` runs both the golden check AND the selftest, so every run proves the assertions still bite. Python 3.11+ stdlib only (`re`, `pathlib`). Purpose: STANDARDS §3 requires tests on money paths; the CTA loop drives every intake conversion, so a silent break here is a silent revenue leak. Also refreshed `tests/README.md` with an "Automated (stdlib)" table naming all three tests + a "Not covered by automation today" section (Playwright / nginx-container smokes).
- **Rung II PROVE — stdlib sitemap.xml + robots.txt smoke test.** `tests/test_seo_files.py` + `make test-seo-files` (wired into `make test` alongside the JSON-LD test). Asserts: (a) `sitemap.xml` parses via `xml.etree.ElementTree` and its root is `<urlset>` in the `http://www.sitemaps.org/schemas/sitemap/0.9` namespace with ≥1 `<url><loc>` that is an absolute `https://` URL; (b) `robots.txt` has a `User-agent:` line and a `Sitemap:` line pointing at an absolute `https://` URL; (c) the robots `Sitemap:` URL == origin-of-sitemap-`<loc>` + `/sitemap.xml`, AND the sitemap-origin AND robots-origin BOTH match `<link rel="canonical">` in `index.html`. Cross-check catches the silent-drift failure mode: someone flips the canonical origin on `index.html` but forgets to update the other two files (or vice versa) — historically a real SEO regression pattern. Python 3.11+ stdlib only (`re`, `xml.etree.ElementTree`, `pathlib`, `urllib.parse`). Passes today. Self-tested against 8 broken inputs (empty urlset, wrong XML namespace, relative `<loc>`, malformed XML, robots missing `Sitemap:`, robots missing `User-agent:`, cross-file canonical drift, missing canonical in index.html) — all 8 correctly rejected. Not a rubber stamp.
- **Rung II PROVE — stdlib JSON-LD smoke test.** `tests/test_jsonld.py` extracts every `application/ld+json` block from `index.html` and asserts (a) it parses, (b) `@context == https://schema.org`, (c) `@type` includes a `LocalBusiness` subtype (`GeneralContractor` in our case), (d) all money-signal fields Google reads for local-pack candidacy are present (`name`, `description`, `url`, `telephone`, `address` with `PostalAddress` shape, `geo` with in-range `GeoCoordinates`, `areaServed`, `priceRange`, `openingHoursSpecification`, `hasOfferCatalog` with children). Python 3.11+ stdlib only (`json`, `re`, `pathlib`) — no pip install, no network. Passes today; self-tested against deliberately-broken blocks (missing fields → 12 errors, wrong `@context`/`@type` → 14 errors). Wired into `Makefile` as `make test-jsonld` (and `make test` now delegates to it). Purpose: catch silent schema regressions in future ticks — this JSON-LD block is a client-visible money signal (Google local-pack candidacy) and STANDARDS §3 requires tests on money paths. Runnable directly: `python tests/test_jsonld.py`.
- **Rung IV QUICKEN — three safe perf micro-wins (no visual change).** (1) `decoding="async"` on all 7 `<img>` tags (1 hero portrait + 6 portfolio) — off-main-thread decode, compounds with existing `loading="lazy"` on the six below-fold cards. (2) `fetchpriority="high"` on the hero `<img>` at `index.html:1806` — matches the existing `<link rel="preload" fetchpriority="high">` at line 43 so Chrome's LCP-element detection is unambiguous instead of relying on layout heuristics. (3) `<link rel="preconnect">` + `<link rel="dns-prefetch">` for `https://formspree.io` (the quote-form action target) — trims TLS + DNS from the primary money-lever's submit critical path. Verified via stdlib `html.parser`: 7 imgs carry `decoding=async`, 2 formspree link hints present. Root-cause bottleneck (206px placeholder images upscaled 3-5×, TODO PARKED §1) is still client-blocked; these micro-wins compound with the async-fonts + hero-preload work from 2026-07-05 block 1.
- **Analytics adapter: `dataLayer` → `window.gtag` / `window.plausible` bridge.** Vanilla-JS IIFE at `index.html` line ~2937 (right after the conversion IIFE from tick 3 that emits `cta_click` + `intake_submit`). Monkey-patches `dataLayer.push` on load: every subsequent push is mirrored to `window.gtag('event', evt.event, props)` and/or `window.plausible(evt.event, { props: props })` whenever either function is defined. No-ops on gtag's own `arguments`-shaped pushes (no `.event` property → skipped) so there's no forwarding loop when both tags are live. `try/catch` around the forward so a broken consumer script never breaks a CTA click. Consequence: the client-blocked half of the previous NEXT ACTION collapses to a single `<script>` tag drop-in — both snippet variants (Plausible + GA4) documented inline in the adapter and in TODO NEXT ACTION.
- **`/robots.txt` + `/sitemap.xml` at repo root.** `robots.txt` is `User-agent: * / Allow: / / Sitemap: https://big7construction.com/sitemap.xml`. `sitemap.xml` (sitemap.org 0.9 namespace) lists the single canonical URL `https://big7construction.com/` — `404.html` is `noindex` and served via nginx `error_page ... internal;`, so deliberately excluded per sitemap.org guidance. Comment inline flags TODO PARKED §4 (service + per-area pages) as the moment to extend the sitemap with real `<url>` entries. Parses via `xml.etree.ElementTree`. `Dockerfile` gains two `COPY` lines; `nginx.conf` gains explicit `location = /robots.txt` + `location = /sitemap.xml` blocks with `Cache-Control: public, max-age=3600` (the server default of 1-year immutable is wrong for crawler-facing files) + the 5 defense-in-depth security headers re-added inline (SECURITY_AUDIT.md § M3-M6 reset pattern, same fix already used on `/index.html`). Smoke: `python -m http.server` returns 200 with correct bytes on both. Closes two long-standing PARKED items in one commit.
- **Real 1200×630 branded OG card + preview-clean meta tags on both top-level pages.** `images/og-card.png` (36KB, `image/png`, 1200×630) generated by `scripts/gen-og-card.py` — a typography card in the site's paper/ink/accent palette (Impact + Bahnschrift on Windows font stack; deterministic byte-for-byte rerun on the same environment). Homepage `og:image` + `twitter:image` swapped from the 206×206 `jobsite-01.jpg` placeholder to the new card, and `og:image:width` (1200), `og:image:height` (630), `og:image:type` (`image/png`) declared so iMessage/Slack/Facebook can render `summary_large_image` without cropping guesswork. `404.html` gained the full OG + Twitter block (same card, page-specific title/description, `og:url` intentionally set to the homepage so a shared broken-link preview still routes visitors to a live page). `noindex` on `404.html` preserved. Verified: `PIL.Image.open('images/og-card.png').size == (1200, 630)`, `format == 'PNG'`. JSON-LD `image` + `logo` intentionally left pointing at `jobsite-01.jpg` (they represent the business per schema.org, not a marketing card). Hero image preload untouched.
- **Conversion audit fix — `data-intent` + form prefill + `cta_click`/`intake_submit` events.** CONVERSION_STANDARDS.md §§ 2 (intent), 3 (prefill), 4 (attribution) gap-fill. Every service-list row (7) and portfolio case-study card (6) now carries a semantic `data-intent` (`service:<slug>` or `portfolio:<slug>`). One IIFE below the existing form-submit handler listens on the document: on any CTA click it (a) pushes `cta_click` (intent, page, label) to `window.dataLayer` — GA4-compatible, no analytics dep required today; (b) selects the matching `input[name=projectType]` radio if none is already checked (never overwrites user input); (c) seeds the textarea with `— Interested in: <label>\n\n` so the visitor lands on a hint-populated form, not a blank one (kills anti-pattern §8 "CTA scrolls to a form that's blank"). Form-submit also fires `intake_submit` with `intent`, `has_prefill` bool, and `message_length` — funnel loop closed per §4. All 13 intent → projectType-value mappings verified against the real radio inputs. `data-intent` count check: 13 CTAs, 13 unique values. Commit `c6edb41`.
- **JSON-LD `geo` + `hasMap` on `GeneralContractor` block.** `GeoCoordinates` (Atlanta 33.7490, -84.3880) and `hasMap` (Google Maps URL) added as top-level properties inside the existing `<script type="application/ld+json">`. These were the last two easy fields Google reads for local-pack candidacy signal. Deliberately did NOT add `sameAs` (no confirmed social handles on file) or `aggregateRating` (no real reviews yet) — LAW 6 blocks fabricating either; both parked in `TODO.md`. JSON validated via `python -c "json.loads(...)"`; two-division `hasOfferCatalog` intact. Commit `b9450ce`.
- **Real `/404.html` + no-more-SPA-shim.** Editorial 404 page in brand type + palette + one CTA + `noindex` meta + fonts preloaded via Filament pattern (no render-block regression). `nginx.conf` `try_files` falls through to `=404`; `error_page 404 /404.html;` with `internal;` location prevents direct access. `Dockerfile` copies the file. Was silently rewriting typos + broken inbound links to `/` with HTTP 200 — bad for SEO, user trust, and analytics honesty. Closes STANDARDS §6 deploy-checklist "404 page exists." Commit `d921703`.
- **Project state scaffolding.** `TODO.md`, `STATUS.md`, `DECISIONS.md` per canon Book I §2 (CONTENT.md and CHANGELOG.md already existed). TODO carries the exact Lighthouse re-measurement micro-steps for a 60-second cold start.

### Changed
- **JSON-LD `image` + `logo` now point at the 1200×630 branded `og-card.png` instead of the 206×206 `jobsite-01.jpg` placeholder (tick 11).** `image` is now an array — `[og-card.png, jobsite-01.jpg]` — so Google's structured-data consumer picks the larger card first while the jobsite photo remains a valid fallback. `logo` is now a `schema.org/ImageObject` with explicit `url`, `contentUrl`, `width: 1200`, `height: 630` (was a bare URL to the 206px placeholder — below Google's 112px organization-logo floor and a poor rich-result surface). Rationale: the branded 1200×630 OG card shipped in tick 4 for social previews was never wired into structured data, so Google's local-pack candidacy and Knowledge-Panel candidates were still using the 206px placeholder. All-Google-guideline sizes met without fabricating a single asset (LAW 6 clean — both files already exist in `images/`, verified by `PIL.Image.open` on the OG card in tick 4).
- **Dark-divider tabular contrast — AA-passing.** Added scoped CSS `.divider.dark .section-marker span.tabular { color: var(--ink-200); }` (index.html line 269). The inherited `--ink-500` (#4C5258) on `--ink-950` (#08090B) at ~3.15 was the last known WCAG AA failure on the audit; `--ink-200` (#CBCFD3) on `#08090B` clears ~11.5. Rule is scoped to `.divider.dark` so light-divider spans (§03–§07 on `--paper`, which already pass) are intentionally untouched. Predicted a11y 93 → 96 or 98. Michael-side Lighthouse re-measure still pending.
- **A11y contrast pass** (predicted Lighthouse a11y 93 → 95/96, needs live re-measure). `.brand-sub` color `--ink-400` (#6E757C) → `--ink-500` (#4C5258) — contrast on paper 4.14 → 5.53. `.btn-accent` background `--accent-500` (#E85D2C) → `--accent-600` (#B34419) — white text contrast 3.48 → 5.05. Hover swapped from a bg swap to `filter: brightness(0.88)` to preserve interaction affordance without a new palette token. All 8 `.section-marker span.tabular` inline `opacity: 0.6` → `0.8` (marginal on light dividers; dark-divider case still fails AA — deferred). Commit `ec70763`.
- **`.hero-stats <dl>` flattened.** Dropped 4 wrapper `<div class="hero-stat">`. `<dt>` and `<dd>` are now direct children per WCAG. CSS `grid-auto-flow: row` places 4 dts across row 1 and 4 dds across row 2 — visual layout preserved. Renamed `.hero-stat *` → `.hero-stats *`; grep-verified zero orphans. Commit `b06b4ba`.
- **CDN correctness.** `nginx.conf` gained `Vary: Accept-Encoding` header — gzip is on, without Vary a shared cache can serve a gzipped response to a client that didn't advertise gzip support. Same commit `ec70763`.

### Not-a-change
- **`Cache-Control` headers on `nginx.conf` were not "broken."** Prior TODO PARKED §3 flagged cache-insight=50 in Lighthouse, but the audit was run against `python -m http.server`, not nginx. The nginx.conf already sets `public, max-age=31536000, immutable` at server scope + `max-age=0, must-revalidate` on `/index.html`. Book I §3: repo beats word; correcting the record.

### Requires Railway redeploy to observe
- `404.html` won't serve until nginx rebuilds. `curl -I https://<prod>/does-not-exist` should return `404` + the 404 body after redeploy.
- `try_files =404` change: same rebuild.
- A11y contrast + `Vary` header: `curl -I https://<prod>/` shows `Vary: Accept-Encoding` after redeploy.

- feat(seo): flesh out social + structured-data metadata on `index.html` — `<link rel="canonical">`, `og:site_name`/`og:url`/`og:image`/`og:image:alt`/`og:locale`, `twitter:title`/`twitter:description`/`twitter:image`/`twitter:image:alt`. Extended the existing `GeneralContractor` JSON-LD block with `image`, `logo`, `openingHoursSpecification` (Mon-Fri 07:00-18:00), and a two-division `hasOfferCatalog` (Commercial & Enterprise / Residential & Repair). Existing name/description/areaServed/telephone/email/priceRange/address kept intact.
- feat(security): 5 defense-in-depth response headers on every response — `Strict-Transport-Security`, `X-Content-Type-Options`, `X-Frame-Options: DENY`, `Referrer-Policy`, `Permissions-Policy`. Closes SECURITY_AUDIT.md § M3-M6 for Big7Construction (out of original audit scope, surfaced by `verify-security-headers.py` live sweep). Headers duplicated in `location = /index.html` because nginx `add_header` inheritance resets when a nested location declares its own directives. Docker build + curl smoke pending next Railway redeploy.

## [0.1.0] - 2026-07-03

Initial documented release. Prior work committed since 2026-06-15 is being
captured retroactively; see `git log` for the full history.

### Added
- README: replace hardcoded 'C:\Users\Michael\Documents\GitHub\' path with relative reference
- feat: add § 07 The Straight Answers FAQ section
- Wire form to Formspree + add SVG favicon
- Palette shift: copper → orange, muted steel → punchy blue
- Redesign to editorial-tier: single-file HTML, editorial typography, corporate GC positioning
- docs: point CLAUDE.md at repo-wide standards + add tests scaffold
- chore: roll out engineering standards + cross-platform scripts
- Add project governance, docs, and CI/CD infrastructure
- chore: apply project standard — add CLAUDE.md, README, .gitignore
- Add light/dark mode toggle to nav
